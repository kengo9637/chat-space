.side-bar
  .side-bar__user-name
    .top-items
      .top-items__name 
        = current_user.name
      .top-items__icon
        = link_to new_group_path do
          =icon('far', 'edit', class: 'top-items__icon__btn-a')
          %span<>
        = link_to edit_user_path(current_user) do
          =icon('fas', 'cog', class: 'top-items__icon__btn-b')
  .side-bar__group-list
    - current_user.groups.each do |group|
      = link_to group_messages_path(group), class: "side-bar__group-list__link" do
        .list-box
          .list-box__name
            = group.name
          .list-box__message
            メッセージはありません